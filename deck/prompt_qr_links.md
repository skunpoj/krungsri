# Prompt-QR links (chatgpt.com) — split into 3 files

This file got too large to read in one shot (mostly long percent-encoded
QR URLs), so it's split into:

1. **`prompt_qr_links_part1_instructions.md`** — context, every test
   case's steps (including the new Test Case 1B, 3, 4), results so far,
   and the coverage matrix. **Read this one first.**
2. **`prompt_qr_links_part2_qr_howto1to3.md`** — per-slide prompt text +
   `chatgpt.com/?q=...` QR URLs for HOW TO 1, 2, 3.
3. **`prompt_qr_links_part3_qr_howto4to5_bonus.md`** — per-slide prompt
   text + QR URLs for HOW TO 4, 5, Bonus 1, Bonus 2.

All three are committed in `deck/` alongside this file.
